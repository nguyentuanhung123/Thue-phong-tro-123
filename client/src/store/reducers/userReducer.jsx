/**
 * Lưu thông tin của người dùng
 */

const initState = {
    userData: {}
}

const userReducer = (state = initState, action) => {
    switch(action.type) {

        default:
            return state;
    }
}

export default userReducer;